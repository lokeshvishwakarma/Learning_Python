#!/usr/bin/python

import module as mc  # import 'module' as 'object'

# every variable,function, class in the imported 'module' appears as 'attribute'
# use them as:	'object.attribute'
print(mc.title)
