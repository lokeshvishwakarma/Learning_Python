#!/usr/bin/python

# Title: Sorting Keys: for Loops

D = {'b': 2, 'a': 1, 'c': 3}
print(D)
ks = list(D.keys())  # unordered keys list
print(ks)
ks = sorted(list(D.keys()))  # sorted keys list
print(ks)
